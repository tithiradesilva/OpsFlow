global:
  scrape_interval: 5s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: "opsflow-app"
    metrics_path: "/actuator/prometheus"
    static_configs:
      - targets: ["app:8081"]